# Инструкция по деплою на Render.com

## Шаг 1: Подготовка репозитория

Убедитесь, что папка `pdf/` с Python-сервисом находится в корне проекта (уже есть).

## Шаг 2: Создание сервиса на Render.com

1. Зайдите на [Render.com Dashboard](https://dashboard.render.com)
2. Нажмите "New +" → "Blueprint"
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml` и создаст сервис

Или создайте сервис вручную:
1. "New +" → "Web Service"
2. Подключите репозиторий
3. Настройки:
   - **Name**: `ikap-backend`
   - **Environment**: `Node`
   - **Build Command**: `npm install && npm run build && cd pdf && pip3 install -r requirements.txt`
   - **Start Command**: `npm start`

## Шаг 3: Настройка переменных окружения

В настройках сервиса (Environment) добавьте следующие переменные:

### Обязательные:

```
OPENAI_API_KEY=ваш_ключ_openai
DATABASE_URL=postgresql://ваша_строка_подключения
ADOBE_CLIENT_ID=ваш_adobe_client_id
ADOBE_CLIENT_SECRET=ваш_adobe_client_secret
```

### Автоматически настроенные через render.yaml:

- `PDF_SERVICE_PATH=./pdf` (уже настроено)
- `PYTHON_PATH=python3` (уже настроено)
- `NODE_ENV=production` (уже настроено)
- `PORT` (генерируется автоматически)

### Опциональные:

```
OPENAI_TIMEOUT_MS=1200000
OPENAI_MAX_RETRIES=2
```

## Шаг 4: Проверка деплоя

После деплоя проверьте логи:

1. **Установка зависимостей Node.js**:
   ```
   npm install
   ```

2. **Сборка фронтенда**:
   ```
   npm run build
   ```

3. **Установка Python зависимостей**:
   ```
   cd pdf && pip3 install -r requirements.txt
   ```

4. **Запуск приложения**:
   ```
   npm start
   ```

## Шаг 5: Тестирование

1. Откройте ваш сервис на Render.com
2. Загрузите тестовый PDF файл через интерфейс
3. Проверьте логи - должны появиться сообщения:
   - `🔄 Конвертирую X PDF файл(ов) в JSON...`
   - `✅ Конвертация завершена: получено X результат(ов)`
   - `📄 Создан JSON файл: ...`

## Устранение проблем

### Ошибка: "Python скрипт завершился с ошибкой"

**Решение:**
1. Проверьте логи установки Python зависимостей
2. Убедитесь, что `pdf/requirements.txt` существует
3. Проверьте, что `ADOBE_CLIENT_ID` и `ADOBE_CLIENT_SECRET` установлены

### Ошибка: "Module not found" при вызове Python

**Решение:**
1. Убедитесь, что Python зависимости установлены в `buildCommand`
2. Проверьте, что путь `PDF_SERVICE_PATH=./pdf` правильный
3. В логах должно быть видно установку зависимостей

### Ошибка: "Adobe API credentials обязательны"

**Решение:**
1. Убедитесь, что `ADOBE_CLIENT_ID` и `ADOBE_CLIENT_SECRET` установлены в Environment Variables
2. Проверьте, что значения не содержат лишних пробелов

### Python зависимости не устанавливаются

**Решение:**
Если `pip3 install` не работает, попробуйте изменить `buildCommand`:
```bash
npm install && npm run build && (cd pdf && python3 -m pip install -r requirements.txt || echo "Python deps skipped")
```

## Альтернативный вариант: Отдельный Python-сервис

Если возникают проблемы с установкой Python зависимостей, можно запустить Python-сервис отдельно:

1. Создайте второй Web Service на Render.com для Python-сервиса
2. Используйте `Environment: Python 3`
3. Установите переменные:
   ```
   USE_PDF_SERVICE_HTTP=true
   PDF_SERVICE_URL=https://ваш-python-сервис.onrender.com
   ```

Но рекомендуется использовать прямой вызов Python скрипта (как настроено в `render.yaml`).

## Проверка работоспособности

После успешного деплоя:

1. ✅ Сервис должен запуститься без ошибок
2. ✅ В логах должно быть: `🚀 Backend iKapitalist запущен на порту ...`
3. ✅ При загрузке PDF должен появиться лог конвертации
4. ✅ JSON файл должен быть создан и отправлен агенту

## Полезные ссылки

- [Render.com Documentation](https://render.com/docs)
- [Python на Render.com](https://render.com/docs/python)
- [Environment Variables](https://render.com/docs/environment-variables)

